[package]
name = "videoson-core"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Core traits and types for videoson: decoders, demuxers, frames, and codec registry"
rust-version.workspace = true

[features]
default = ["std"]
std = []

[dependencies]
